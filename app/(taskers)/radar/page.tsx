import SpatialTaskGrid from "@/components/spatial/SpatialTaskGrid";

export const metadata = {
  title: "Tasker Radar — EKIRO",
};

export default function RadarPage() {
  return <SpatialTaskGrid />;
}
